# 控件层

::: info 该文档编写者
墨北MovTery - 2025/10/29
:::

控件层是 [控件](/docs/control2_help/controls) 的承载区域，所有控件都必须放置在控件层上。  
类似于绘图软件中的“图层”，控件层可以帮助您轻松实现以下功能：  
- 通过不同的控件层对控件进行管理和组织。  
- 实现控件的分层显示、隐藏或独立操作。

控件层无大小概念，永远铺满整个屏幕。

## 创建控件层

在右侧菜单中，您可以点击 “新建” 按钮创建新的控件层。  

![创建控件层](/zh/docs/control2_help/control_layer/create_new_layer.png)  

您可以点击某控件层选中它，选中的控件层会以粗边框显示。  
要在控件层上添加控件，需先选中该控件层。  

控件层拥有先后顺序，按照从底向顶的顺序依次加载，您可以拖动控件层对它们进行排序。  

![排序](/zh/docs/control2_help/control_layer/ordering.gif)

控件层拥有独立的属性，您可以利用这些属性制作强大的功能：  

![控件层属性](/zh/docs/control2_help/control_layer/control_layer_attributes.png)  

## 控件层属性

- **可见场景**  
  关于游戏可见场景，详细了解：[游戏状态](/docs/control2_help/control_layout_operations#游戏状态)  
  - **游戏内可见：**  
    游戏进入无鼠标操作模式时，该控件层可见  
  - **游戏外可见：**  
    游戏进入鼠标操作模式时，如打开 GUI，该控件层可见  
- **默认隐藏控件层**  
  默认情况下，不显示该控件层，将来只能通过按钮的点击事件来切换该控件层的可见性
- **在实体鼠标操作时不隐藏**  
  当您使用实体鼠标时，如果此项启用，则不隐藏该控件层  
  关于控制布局操作，详细了解：[控制布局操作](/docs/control2_help/control_layout_operations#操作状态)
- **在手柄操作时不隐藏**  
  当您使用手柄时，如果此项启用，则不隐藏该控件层  
  关于控制布局操作，详细了解：[控制布局操作](/docs/control2_help/control_layout_operations#操作状态)
- **合并控件至下层**  
  将该控件层的所有控件，全部合并到下一层控件层
