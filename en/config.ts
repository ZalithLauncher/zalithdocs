import { defineAdditionalConfig } from 'vitepress';
import en from '../locales/en';

export default defineAdditionalConfig({
  lang: 'en-US',
  description: "Zalith Launcher is a Minecraft launcher based on PojavLauncher, which can play Minecraft: Java Edition on Android devices.",

  themeConfig: en
});